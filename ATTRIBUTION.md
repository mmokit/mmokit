# Attribution

Third-party assets bundled in this repository, and their licences. Everything
not listed here is original work covered by [`LICENSE`](LICENSE) (MIT).

## Audio — `examples/space/web/public/audio/`

> **Status: unresolved. These files are NOT covered by the MIT grant.**
>
> Sixteen Ogg files were added in a single commit (`e6f5d6af`, "soundsz!")
> with no recorded source. Their embedded Vorbis comments are the only
> evidence: four carry `ARTIST=KenneyG`, eleven carry `COMMENTS=Sound
> generated by GameSynth from Tsugi`, one carries only LMMS's default export
> string, and four carry nothing. A metadata tag is an authorship claim, not
> a licence grant.
>
> The resolution is replacement: each file is being swapped for audio with a
> documented CC0 source, keeping the same filename so no code changes
> (`SoundId` in `examples/space/web/src/audio/sounds.ts` keys by filename).
> Until every row below names a source and a licence, this repository should
> not be published.

| File | Source | Author | Licence |
| --- | --- | --- | --- |
| `music/space-ambient.ogg` | *unresolved* | — | — |
| `sfx/afterburner.ogg` | *unresolved* | — | — |
| `sfx/explosion.ogg` | *unresolved* | — | — |
| `sfx/extract-pulse.ogg` | *unresolved* | — | — |
| `sfx/hit-impact.ogg` | *unresolved* | — | — |
| `sfx/ion-burn.ogg` | *unresolved* | — | — |
| `sfx/loot-pickup.ogg` | *unresolved* | — | — |
| `sfx/mining-laser.ogg` | *unresolved* | — | — |
| `sfx/missile-launch.ogg` | *unresolved* | — | — |
| `sfx/plasma-torpedo.ogg` | *unresolved* | — | — |
| `sfx/railgun-fire.ogg` | *unresolved* | — | — |
| `sfx/shield-activate.ogg` | *unresolved* | — | — |
| `sfx/target-lock.ogg` | *unresolved* | — | — |
| `sfx/thruster.ogg` | *unresolved* | — | — |
| `sfx/tractor-beam.ogg` | *unresolved* | — | — |
| `sfx/ui-click.ogg` | *unresolved* | — | — |

## Everything else

There is nothing else. Verified with git's own binary detection over every
tracked file: the sixteen `.ogg` files above are the only non-source assets in
the repository. All game art is drawn procedurally with PixiJS `Graphics` —
zero `Assets.load`, zero `Texture.from`, zero image, sprite, or font files.

## Dependencies

Go, JavaScript and C# dependencies are declared in `go.mod`, the `package.json`
files, and the `.csproj` files respectively, and are fetched rather than
vendored — no third-party source is copied into this tree. Every Go dependency
is MIT, BSD-3-Clause, Apache-2.0 or ISC. The only copyleft anywhere in the
transitive JavaScript graph is MPL-2.0 `lightningcss`, a build-time CSS
transformer used by the admin dashboard's Tailwind pipeline; it is not
redistributed.
