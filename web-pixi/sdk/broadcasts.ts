// GENERATED by sdkgen — do not edit.

// Typed broadcast events. Wire format mirrors the reflect-codec layout
// in pkg/universe/reflect_marshal.go: fields encoded in source order,
// little-endian, no padding. mmokit.Entity → 4-byte NetID.

/** Broadcast-eligible event game.BeamToggle (typeID 0xc6de6e60). */
export class BeamToggle {
  static readonly typeID = 0xc6de6e60;
  caster: number = 0;
  beam: number = 0;
  active: boolean = false;

  static decode(buf: Uint8Array): BeamToggle {
    const dv = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
    let off = 0;
    const m = new BeamToggle();
    m.caster = dv.getUint32(off, true); off += 4;
    m.beam = dv.getUint8(off); off += 1;
    m.active = dv.getUint8(off) !== 0; off += 1;
    return m;
  }
}

/** Broadcast-eligible event game.Damage (typeID 0x80c8b22a). */
export class Damage {
  static readonly typeID = 0x80c8b22a;
  amount: number = 0;
  bonusDamage: number = 0;
  slot: number = 0;
  abilityType: number = 0;
  source: number = 0;
  target: number = 0;
  dealt: number = 0;
  killed: boolean = false;

  static decode(buf: Uint8Array): Damage {
    const dv = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
    let off = 0;
    const m = new Damage();
    m.amount = dv.getFloat32(off, true); off += 4;
    m.bonusDamage = dv.getFloat32(off, true); off += 4;
    m.slot = dv.getUint8(off); off += 1;
    m.abilityType = dv.getUint8(off); off += 1;
    m.source = dv.getUint32(off, true); off += 4;
    m.target = dv.getUint32(off, true); off += 4;
    m.dealt = dv.getFloat32(off, true); off += 4;
    m.killed = dv.getUint8(off) !== 0; off += 1;
    return m;
  }
}

/** Broadcast-eligible event game.Killed (typeID 0x6e8c9b2a). */
export class Killed {
  static readonly typeID = 0x6e8c9b2a;
  killer: number = 0;
  target: number = 0;

  static decode(buf: Uint8Array): Killed {
    const dv = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
    let off = 0;
    const m = new Killed();
    m.killer = dv.getUint32(off, true); off += 4;
    m.target = dv.getUint32(off, true); off += 4;
    return m;
  }
}

/** Broadcast-eligible event game.MineExtract (typeID 0xa5dd8b79). */
export class MineExtract {
  static readonly typeID = 0xa5dd8b79;
  caster: number = 0;
  asteroid: number = 0;
  beam: number = 0;
  requestedAmt: number = 0;
  extracted: number = 0;
  depleted: boolean = false;

  static decode(buf: Uint8Array): MineExtract {
    const dv = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
    let off = 0;
    const m = new MineExtract();
    m.caster = dv.getUint32(off, true); off += 4;
    m.asteroid = dv.getUint32(off, true); off += 4;
    m.beam = dv.getUint8(off); off += 1;
    m.requestedAmt = dv.getFloat32(off, true); off += 4;
    m.extracted = dv.getFloat32(off, true); off += 4;
    m.depleted = dv.getUint8(off) !== 0; off += 1;
    return m;
  }
}

/** Broadcast-eligible event game.Status (typeID 0xc5ec6d9d). */
export class Status {
  static readonly typeID = 0xc5ec6d9d;
  effectType: number = 0;
  duration: number = 0;
  value: number = 0;
  slot: number = 0;
  abilityType: number = 0;
  source: number = 0;
  target: number = 0;

  static decode(buf: Uint8Array): Status {
    const dv = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
    let off = 0;
    const m = new Status();
    m.effectType = dv.getUint8(off); off += 1;
    m.duration = dv.getFloat32(off, true); off += 4;
    m.value = dv.getFloat32(off, true); off += 4;
    m.slot = dv.getUint8(off); off += 1;
    m.abilityType = dv.getUint8(off); off += 1;
    m.source = dv.getUint32(off, true); off += 4;
    m.target = dv.getUint32(off, true); off += 4;
    return m;
  }
}

/** Broadcast-eligible event game.Docked (typeID 0x8bf5541d). */
export class Docked {
  static readonly typeID = 0x8bf5541d;

  static decode(buf: Uint8Array): Docked {
    const dv = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
    let off = 0;
    const m = new Docked();
    return m;
  }
}

/** Broadcast-eligible event game.DockingState (typeID 0x5c80df11). */
export class DockingState {
  static readonly typeID = 0x5c80df11;
  docking: boolean = false;
  progress: number = 0;
  totalTime: number = 0;
  stationID: number = 0;

  static decode(buf: Uint8Array): DockingState {
    const dv = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
    let off = 0;
    const m = new DockingState();
    m.docking = dv.getUint8(off) !== 0; off += 1;
    m.progress = dv.getFloat32(off, true); off += 4;
    m.totalTime = dv.getFloat32(off, true); off += 4;
    m.stationID = dv.getUint32(off, true); off += 4;
    return m;
  }
}

/** Broadcast-eligible event game.PlayerDied (typeID 0xef56f740). */
export class PlayerDied {
  static readonly typeID = 0xef56f740;
  killerID: number = 0;

  static decode(buf: Uint8Array): PlayerDied {
    const dv = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
    let off = 0;
    const m = new PlayerDied();
    m.killerID = dv.getUint32(off, true); off += 4;
    return m;
  }
}

/** Dispatches incoming TypedEvent (typeID + body bytes) to typed handlers. */
export class TypedDispatcher {
  private handlers = new Map<number, (raw: Uint8Array) => void>();

  /** Register a handler for broadcasts of class T. The class must expose
   *  static readonly typeID and static decode(buf): T. */
  on<T>(cls: { typeID: number; decode(buf: Uint8Array): T }, fn: (msg: T) => void): () => void {
    const handler = (raw: Uint8Array) => fn(cls.decode(raw));
    this.handlers.set(cls.typeID, handler);
    return () => { if (this.handlers.get(cls.typeID) === handler) this.handlers.delete(cls.typeID); };
  }

  /** Dispatch one incoming wire event. No-op if no handler is registered for typeID. */
  dispatch(typeID: number, body: Uint8Array): void {
    const fn = this.handlers.get(typeID);
    if (fn) fn(body);
  }
}
